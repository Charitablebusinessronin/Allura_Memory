# Bun configuration for Allura Memory
# Resolves TypeScript path aliases for direct Bun execution

[install]
# Use exact versions for reproducibility
exact = true

[run]
# Preload script to set up module resolution
# This ensures path aliases work when running TS files directly
preload = ["./scripts/bun-preload.ts"]

[test]
# Test configuration
coverage = true
